import{ShieldCheck,Users,Package,Receipt}from"lucide-react";import{useEffect,useState}from"react";import{Link}from"react-router-dom";import Page from"../components/Page";import{useAuth}from"../context/AuthContext";import{supabase}from"../lib/supabase";
export default function Admin(){const{profile}=useAuth();const[s,setS]=useState({users:0,products:0,orders:0});useEffect(()=>{(async()=>{const[a,b,c]=await Promise.all([supabase.from("profiles").select("id",{count:"exact",head:true}),supabase.from("products").select("id",{count:"exact",head:true}),supabase.from("orders").select("id",{count:"exact",head:true})]);setS({users:a.count||0,products:b.count||0,orders:c.count||0})})()},[]);return <Page className="mx-auto w-[min(1180px,calc(100%-28px))] py-12"><p className="text-xs font-black uppercase tracking-widest text-violet-300">Administration</p><h1 className="mt-3 text-5xl font-black">Control center.</h1><p className="mt-3 text-zinc-500">Role-protected operational overview for {profile?.display_name||"admin"}.</p><div className="mt-8 grid gap-4 sm:grid-cols-3">{[[Users,"Users",s.users], [Package,"Products",s.products], [Receipt,"Orders",s.orders]].map(([I,n,v])=><div key={n} className="glass rounded-2xl p-6"><I className="text-violet-300"/><p className="mt-5 text-xs text-zinc-500">{n}</p><b className="mt-1 block text-3xl">{v}</b></div>)}</div><div className="mt-6 rounded-2xl border border-amber-400/10 bg-amber-400/5 p-5 text-sm text-zinc-400"><ShieldCheck className="mb-3 text-amber-300"/><b className="text-zinc-200">Security boundary:</b> admin access is enforced by the profile role plus Supabase RLS. Never rely on this UI alone for authorization.</div><div className="mt-6 flex flex-wrap gap-3"><Link className="rounded-xl border border-white/10 px-4 py-3 text-sm font-bold" to="/dashboard/products">Seller products</Link><Link className="rounded-xl border border-white/10 px-4 py-3 text-sm font-bold" to="/dashboard/orders">Payment review</Link></div></Page>}
