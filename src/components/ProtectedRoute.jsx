import{Navigate,Outlet}from"react-router-dom";import{useAuth}from"../context/AuthContext";export default function ProtectedRoute({roles}){const{user,profile,loading}=useAuth();if(loading)return <div className="grid min-h-screen place-items-center bg-[#07070b]"><span className="size-8 animate-spin rounded-full border-2 border-white/20 border-t-violet-400"/></div>;if(!user)return <Navigate to={roles?.includes("admin")?"/admin/login":"/login"} replace/>;if(roles&&profile===null)return <div className="grid min-h-screen place-items-center bg-[#07070b]"><span className="size-8 animate-spin rounded-full border-2 border-white/20 border-t-violet-400"/></div>;if(roles&&!roles.includes(profile?.role))return <Navigate to="/dashboard" replace/>;return <Outlet/>}
